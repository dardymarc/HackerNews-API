{
  "name": "hackernews-fullstack",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",

    "frontend": "cd frontend && npm install && npm start",

    "backend": "cd backend/HackerNewsApi && dotnet run"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
